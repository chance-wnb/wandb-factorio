[package]
name = "rust_client"
version = "0.1.0"
edition = "2021"

[dependencies]
wandb = { path = "${WANDB_SDK_PATH}" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
